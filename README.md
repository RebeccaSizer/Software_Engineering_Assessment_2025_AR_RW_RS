# Software_Engineering_Assessment_2025_AR_RW_RS
This project is a prototype tool for annotating germline variant data using information from ClinVar. It is designed to support clinical researchers (not bioinformaticians) working with experimental variant data from the Parkinson’s disease panel.

To activate the virtual Conda environment:

conda env create -f environment.yml

Then activate the virtual Conda environment:

conda activate sea_venv